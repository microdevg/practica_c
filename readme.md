## Ordenar alfabeticamente usando una lista enlazada
Para ejecutar el codigo 
```
sudo chmod +x start.sh
./start.sh
```
Te pedira el nombre del archivo con las palabras. Si no existe el codigo no se ejecutara.
